algoritmo exercicio6
    real: valora,valorb,total
    início
        escreva("Digite o comprimento de A: ")
        leia(valora)
        escreva("Digite o valor de B: ")
        leia(valorb)

        valorc = raiz(ladoa*2 + ladob*2)

        total = ladoa + ladob + ladoc

        escreva("A quantidade de metros necessários para fechar a cerca é: " +total)
    fim